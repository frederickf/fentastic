export const alpha = 'alpha'
export const isAlpha = type => type === alpha

export const slash = 'slash'
export const isSlash = type => type === slash

export const ws = 'whitespace'
export const isWhiteSpace = type => type === ws

export const dash = 'dash'
export const isDash = type => type === dash

export const digit = 'digit'
export const isDigit = type => type === digit
